Act as an Expert C++20 and Python 3.12 Software Architect and Senior Developer. 
We are building a Secure File Transfer Client-Server System (Maman 06 - Defensive Systems Programming).

=== PROJECT CONSTRAINTS & PRINCIPLES ===
1. Architecture: OOP, SOLID principles, highly modular.
2. Defensive Programming: Strict input validation, explicit bounds checking, exception safety (RAII in C++), and graceful error handling without crashes.
3. Code Quality: NO MAGIC NUMBERS (use constexpr/enums in C++, constants/Enums in Python). Self-documenting names.
4. Protocol Rules: Binary TCP, strictly Little-Endian for all numeric fields. 23-byte fixed header (ClientID: 16b, Version: 1b, Code: 2b, PayloadSize: 4b).
5. Tech Stack:
   - Client: C++20 (MSVC/CMake), boost::asio (TCP via vcpkg), Crypto++ (RSA 1024, AES-CBC 256).
   - Server: Python 3.12, PyCryptodome, SQLite3 (defensive.db), multi-threading.

=== COMPLETED WORK & CURRENT STATE ===
- **Client Networking & Protocol**: `NetworkManager` implemented via `boost::asio` with strict RAII. `Protocol.h` contains strictly packed structs (`#pragma pack(push,1)`).
- **Server DB & Config**: `server.py` successfully reads `port.info` (fallback to 1369) and initializes `defensive.db` (clients and files tables) using `sqlite3`.
- **Identified Gaps to Fix**: 
  1. Typo in `ClientFileManager.h` (`transfer.jason` -> `transfer.json`).
  2. `main.cpp` attempts to save `me.info` before connecting and retrieving the UUID. Flow needs restructuring.

=== ROADMAP & STAGES ===
**Phase 1: Flow Correction & Server Network Loop**
[1.1] Client: Fix `ClientFileManager` typos and JSON parsing logic (support spaces in client names).
[1.2] Server: Implement the TCP Threaded socket listener in `server.py` `start()` function and define the binary unpacking logic (`struct` module).

**Phase 2: Registration Logic (Req 825)**
[2.1] Client: Restructure `main.cpp` -> Connect -> Send Req 825 (Register).
[2.2] Server: Receive Req 825 -> Validate -> Generate 16-byte UUID -> Save to DB -> Send Res 1600.
[2.3] Client: Receive Res 1600 -> Generate RSA Keys -> Save full `me.info`.

**Phase 3: Key Exchange (Req 826 / Req 827)**
[3.1] Client: Send Req 826 with Public Key.
[3.2] Server: Receive Req 826 -> Generate AES-256 key -> Encrypt with Public Key -> Update DB -> Send Res 1602.
[3.3] Client+Server: Handle Reconnect logic (Req 827 -> Res 1605/1606).

**Phase 4: File Transfer & Integrity (Req 828, 900, 901, 902)**
[4.1] Client: Encrypt file chunks with AES -> Send Req 828.
[4.2] Server: Decrypt chunks -> Calculate cksum -> Send Res 1603.
[4.3] Client+Server: CRC verification and retry logic.

=== CURRENT TASK ===
Please acknowledge this updated roadmap. 
We are starting with **Phase 1: Flow Correction & Server Network Loop**.
1. Provide the necessary code corrections for `ClientFileManager.cpp` (safe JSON parsing) and `ClientFileManager.h`.
2. Provide the Python code for `server.py` to implement the `start()` socket listener, thread dispatching, and a skeleton `ClientHandler` class to process incoming connections.